[project]
name = "ops-mcp-server"
version = "0.1.0"
description = "Read-only server-ops MCP server (Streamable HTTP) for an MCP trial"
requires-python = ">=3.10"
dependencies = [
    "mcp[cli]>=2,<3",
    "psutil>=5.9",
]

[tool.uv]
# アプリケーションであり配布パッケージではないので、プロジェクト自体はビルド・インストールしない。
package = false
